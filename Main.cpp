#include "view/menu.h"

int main() {
    readEFromFile(elist);   //first read it from file, if there are any old data in file
    firstboard(); //then run for firstboard
    writeEToFile(elist);  //write to file for some code that related tosome function
    return 0;
}
